# AIROS

AIROS is developed through isolated major-version directories.

- V1.0 — historical version
- V2.0 — frozen baseline
- V3.0 — active development

Each major version is maintained in its own dedicated directory.
